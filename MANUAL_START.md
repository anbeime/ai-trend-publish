# 🔧 手动启动指南

## 🚨 如果启动脚本失败，请按以下步骤手动操作

### 第1步：检查环境

1. **检查Node.js安装**
   ```bash
   # 打开命令提示符（Win+R，输入cmd）
   node --version
   ```
   
   如果显示版本号（如v18.17.0），说明已安装
   如果提示"不是内部或外部命令"，需要先安装Node.js

2. **安装Node.js（如果需要）**
   - 访问：https://nodejs.org/
   - 下载LTS版本（推荐）
   - 安装时选择"添加到PATH"
   - 安装后重启电脑

### 第2步：准备项目

1. **打开命令提示符**
   ```bash
   # Win+R，输入cmd，回车
   ```

2. **进入项目目录**
   ```bash
   cd C:\D\ai-trend-publish
   ```

3. **验证目录**
   ```bash
   dir
   ```
   应该能看到 wechat-publisher.html 等文件

### 第3步：安装依赖

```bash
npm install
```

如果安装失败，尝试：
```bash
# 清理缓存
npm cache clean --force

# 使用淘宝镜像
npm install --registry https://registry.npmmirror.com

# 或者使用yarn
npm install -g yarn
yarn install
```

### 第4步：启动服务

#### 方案A：简单HTTP服务器（推荐新手）

```bash
npx http-server -p 3000
```

然后访问：
- http://localhost:3000/wechat-publisher.html
- http://localhost:3000/simple-test.html

#### 方案B：如果npx失败，安装全局

```bash
npm install -g http-server
http-server -p 3000
```

#### 方案C：使用Python（如果Node.js有问题）

```bash
# Python 3
python -m http.server 3000

# Python 2
python -m SimpleHTTPServer 3000
```

#### 方案D：使用Live Server（VSCode扩展）

1. 安装VSCode
2. 安装Live Server扩展
3. 右键点击wechat-publisher.html
4. 选择"Open with Live Server"

### 第5步：验证启动

成功启动后会看到类似信息：
```
Starting up http-server, serving ./
Available on:
  http://127.0.0.1:3000
  http://192.168.1.x:3000
Hit CTRL-C to stop the server
```

## 🆘 常见问题解决

### 问题1：端口3000被占用

```bash
# 使用其他端口
npx http-server -p 8080

# 然后访问 http://localhost:8080
```

### 问题2：权限问题

```bash
# 使用管理员权限运行命令提示符
# 右键点击"命令提示符" → "以管理员身份运行"
```

### 问题3：网络问题

```bash
# 检查网络连接
ping nodejs.org

# 使用代理
npm config set proxy http://你的代理地址:端口
npm config set https-proxy http://你的代理地址:端口
```

### 问题4：Node.js版本过低

```bash
# 查看当前版本
node --version

# 更新到最新LTS版本
# 下载 https://nodejs.org/
```

## 🎯 成功标志

如果看到以下情况，说明启动成功：

1. **命令行显示服务器信息**
2. **浏览器能打开 http://localhost:3000**
3. **页面正常显示，没有404错误**
4. **可以点击测试按钮**

## 📱 测试步骤

1. **打开测试页面**
   ```
   http://localhost:3000/simple-test.html
   ```

2. **检查服务器状态**
   - 点击"测试本地服务器"按钮
   - 应该显示"✅ 本地服务器正常"

3. **测试微信API**
   - 输入AppID和AppSecret
   - 点击"直接测试微信API"
   - 应该显示成功信息

4. **使用完整工具**
   ```
   http://localhost:3000/wechat-publisher.html
   ```

## 🔄 每次使用

以后每次使用时：

1. 打开命令提示符
2. 进入项目目录：`cd C:\D\ai-trend-publish`
3. 启动服务：`npx http-server -p 3000`
4. 打开浏览器访问相关页面

---

**还是不行？** 请提供具体的错误信息，我会帮你进一步解决！