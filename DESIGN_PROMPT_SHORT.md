# 🎨 全页面统一风格设计提示词（极简版）

## 🔹 核心规范（所有页面）
```prompt
移动端小程序UI，平面插画风格，紫粉渐变体系：
• 配色：主色#9333EA→#EC4899(顶部/选中/按钮)，背景#F8F9FA，橙色#F97316(热度)
• 排版：按钮文字绝对居中(flex布局)，内边距≥12px
• 布局：卡片圆角16-20px，白色+阴影，间距统一
• 列表：单列流；模板中心一行2列网格
```

## 🔹 分页面追加
### 1. 自媒体/热点创作
```prompt
- 顶部紫粉渐变
- 热点卡片：圆形序号，横向进度条(橙/紫)
- 按钮：「立即创作」紫粉渐变
```

### 2. AI短视频创作
```prompt
- 4个功能图标卡片
- 胶囊标签：「全部」紫粉渐变，其他浅灰
```

### 3. 模板中心
```prompt
- 一行2列网格
- 卡片：插画图标+浅粉标签
- 按钮：「使用」紫粉渐变
```

## 🔧 强制约束（必须加）
```prompt
强制：复用同一套规范，禁止新增颜色/图标；按钮用flex绝对居中；无多余空白。
```

---

## 📋 一键完整版（复制这个给AI）
```prompt
移动端小程序UI，平面插画风格，紫粉渐变体系：
• 配色：主色#9333EA→#EC4899(顶部/选中/按钮)，背景#F8F9FA，橙色#F97316(热度)
• 排版：按钮文字绝对居中(flex布局)，内边距≥12px
• 布局：卡片圆角16-20px，白色+阴影，间距统一
• 列表：单列流；模板中心一行2列网格

【页面适配】
- 自媒体/热点：顶部紫粉渐变；热点卡片圆形序号+进度条；「立即创作」按钮紫粉渐变
- AI短视频：4个功能图标卡片；胶囊标签(全部紫粉渐变/其他浅灰)
- 模板中心：一行2列网格；卡片插画图标+浅粉标签；「使用」按钮紫粉渐变

强制：复用同一套规范，禁止新增颜色/图标；按钮用flex绝对居中；无多余空白。
```

---

## 🎯 检查要点
1. ✅ 所有顶部栏：紫粉渐变
2. ✅ 所有按钮：文字绝对居中
3. ✅ 所有卡片：圆角16-20px
4. ✅ 页面背景：#F8F9FA
5. ✅ 模板中心：一行2列网格
6. ✅ 无多余空白，层级清晰

---

**复制提示**：直接复制上面的「一键完整版」给AI设计工具即可。