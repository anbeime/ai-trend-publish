# AI API 服务自动启动配置指南

## 📋 概述

本指南提供**3种自动启动方案**，让 AI API 服务在需要时自动运行，无需手动操作。

---

## 🎯 方案对比

| 方案 | 启动时机 | 适用场景 | 难度 |
|------|---------|---------|------|
| **方案1: Claude Code Hooks** | Claude Code 启动时 | 推荐⭐⭐⭐ | 简单 |
| **方案2: Windows 开机自启** | 系统开机时 | 长期后台运行 | 中等 |
| **方案3: 手动启动** | 需要时手动 | 临时使用 | 最简单 |

---

## 🚀 方案1: Claude Code Hooks（推荐）

### 特点
- ✅ Claude Code 启动时自动启动 API 服务
- ✅ 自动检测服务是否已运行（避免重复启动）
- ✅ 异步启动（不阻塞 Claude Code）
- ✅ 日志记录到 `~/.claude/ai-api.log`

### 配置步骤

#### 步骤1: 已创建的文件

```
.claude/
├── hooks.json                    # Hooks 配置
├── hooks/
│   ├── auto-start-api.bat       # Windows 启动脚本
│   └── auto-start-api.sh        # Linux/Mac 启动脚本
└── ai-api.log                    # 日志文件（自动创建）
```

#### 步骤2: 配置 Claude Code

Claude Code 会自动读取 `.claude/hooks.json` 文件。配置内容：

```json
{
  "hooks": {
    "session-start": {
      "description": "Claude Code 启动时自动启动 AI API 服务",
      "command": "bash .claude/hooks/auto-start-api.sh",
      "windows_command": ".claude\\hooks\\auto-start-api.bat",
      "enabled": true,
      "async": true
    }
  }
}
```

#### 步骤3: 测试

```bash
# 1. 退出 Claude Code（如果正在运行）
# 2. 重新启动 Claude Code
# 3. 检查服务状态
.claude/check-api-health.bat    # Windows
bash .claude/check-api-health.sh # Linux/Mac
```

#### 步骤4: 查看日志

```bash
# Windows
type C:\Users\13632\.claude\ai-api.log

# Linux/Mac
cat ~/.claude/ai-api.log
```

### 工作原理

```
Claude Code 启动
    ↓
检测 hooks.json
    ↓
执行 session-start hook
    ↓
运行 auto-start-api 脚本
    ↓
检查服务是否已运行
    ↓
未运行 → 启动服务（后台）
已运行 → 跳过
    ↓
记录日志
```

---

## 🖥️ 方案2: Windows 开机自启动

### 特点
- ✅ 系统开机时自动启动
- ✅ 静默启动（无窗口）
- ✅ 一直后台运行
- ⚠️ 占用系统资源

### 配置步骤

#### 方法A: 启动文件夹（推荐）

1. **复制 VBS 脚本到启动文件夹**

```bash
# Windows + R 打开运行对话框
输入: shell:startup
回车

# 将以下文件复制到打开的文件夹
C:\D\ai-trend-publish\.claude\startup-silent.vbs
```

2. **重启电脑测试**

#### 方法B: 任务计划程序（更灵活）

1. **打开任务计划程序**
   - `Win + R` → 输入 `taskschd.msc` → 回车

2. **创建基本任务**
   - 点击 "创建基本任务"
   - 名称: `AI API 服务自动启动`
   - 触发器: `当我登录时`
   - 操作: `启动程序`
   - 程序: `C:\D\ai-trend-publish\.claude\startup-silent.vbs`
   - 完成

3. **高级设置**
   - 双击创建的任务
   - "条件" 标签页 → 取消勾选 "只有在计算机使用交流电源时才启动此任务"
   - "设置" 标签页 → 勾选 "如果任务失败，重新启动" → 间隔: 1分钟

### 停止开机自启

**方法A清理**:
```bash
# 删除启动文件夹中的 .vbs 文件
Win + R → shell:startup → 删除 startup-silent.vbs
```

**方法B清理**:
```bash
# 任务计划程序中禁用或删除任务
Win + R → taskschd.msc → 找到任务 → 禁用/删除
```

---

## 🖱️ 方案3: 手动启动

### 快速启动

```bash
# Windows
.claude\start-ai-api.bat

# Linux/Mac
bash .claude/start-ai-api.sh
```

### 检查状态

```bash
# Windows
.claude\check-api-health.bat

# Linux/Mac
bash .claude/check-api-health.sh
```

---

## 🛠️ 工具说明

### 1. 健康检查工具

```bash
# 检查服务状态、可用模型、测试响应
.claude/check-api-health.bat    # Windows
bash .claude/check-api-health.sh # Linux/Mac
```

**输出示例**:
```
🔍 检查 AI API 服务状态...

✅ 服务状态: 运行中

📋 可用模型:
  1. gpt-5
  2. claude-4.5-sonnet
  3. gemini-2.5-pro
  ... (共24个)

🎉 总计 24 个模型可用

🧪 测试 Gemini 2.5 Flash...
✅ 模型响应正常
```

### 2. 自动启动脚本

```bash
# Claude Code hooks 使用
.claude/hooks/auto-start-api.bat    # Windows
.claude/hooks/auto-start-api.sh     # Linux/Mac
```

**功能**:
- ✅ 检测服务状态
- ✅ 未运行则启动
- ✅ 已运行则跳过
- ✅ 记录日志

### 3. 静默启动脚本

```bash
# 开机自启使用（无窗口）
.claude/startup-silent.vbs
.claude/startup-silent.bat
```

---

## 📊 方案推荐

### 场景1: 日常开发（推荐方案1）
- 使用 Claude Code Hooks
- 需要时自动启动
- 不占用开机资源

### 场景2: 重度用户（方案2）
- 开机自启动
- 一直后台运行
- 随时可用

### 场景3: 临时使用（方案3）
- 手动启动
- 完全控制
- 不需要时关闭

---

## 🔧 故障排除

### 问题1: Hooks 未生效

**检查**:
```bash
# 查看 hooks.json 配置
cat .claude/hooks.json

# 查看日志
cat ~/.claude/ai-api.log
```

**解决**:
- 确认 `hooks.json` 在项目根目录的 `.claude/` 下
- 确认 `enabled: true`
- 重启 Claude Code

### 问题2: 服务启动失败

**检查**:
```bash
# 查看日志
type C:\Users\13632\.claude\ai-api.log

# 手动启动测试
cd C:\D\cursorweb2api
python main.py
```

**解决**:
- 检查 Python 是否安装
- 检查端口 8000 是否被占用
- 检查 `cursorweb2api` 目录是否存在

### 问题3: 开机自启不工作

**检查**:
```bash
# 验证 VBS 文件位置
dir "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"

# 手动运行 VBS 测试
C:\D\ai-trend-publish\.claude\startup-silent.vbs
```

**解决**:
- 确认文件路径正确
- 检查 Python 路径
- 查看任务计划程序日志

### 问题4: 服务重复启动

**现象**: 多个服务实例运行

**解决**:
```bash
# 1. 查找所有 Python 进程
tasklist | findstr python

# 2. 结束旧进程
taskkill /F /PID <进程ID>

# 3. 重新启动
.claude\start-ai-api.bat
```

---

## 📝 日志位置

- **启动日志**: `C:\Users\13632\.claude\ai-api.log`
- **服务输出**: 同一个日志文件
- **错误信息**: 同一个日志文件

**查看最新日志**:
```bash
# Windows (PowerShell)
Get-Content C:\Users\13632\.claude\ai-api.log -Tail 50

# Windows (CMD)
type C:\Users\13632\.claude\ai-api.log

# Linux/Mac
tail -f ~/.claude/ai-api.log
```

---

## ✅ 最佳实践

1. **推荐使用方案1（Claude Code Hooks）**
   - 按需启动，节省资源
   - 自动化程度高
   - 无需手动操作

2. **定期检查日志**
   ```bash
   cat ~/.claude/ai-api.log
   ```

3. **定期健康检查**
   ```bash
   .claude/check-api-health.bat
   ```

4. **遇到问题先看日志**
   - 大部分问题都会在日志中体现

---

## 🎉 总结

现在您有**3种自动启动方案**：

| 方案 | 命令 | 说明 |
|------|------|------|
| **方案1** | 自动触发 | Claude Code 启动时 ⭐ |
| **方案2** | 自动触发 | 系统开机时 |
| **方案3** | `.claude\start-ai-api.bat` | 手动启动 |

**推荐配置**: 方案1（Claude Code Hooks）

**验证**: `.claude\check-api-health.bat`

---

## 📞 支持

如有问题：
1. 查看日志：`C:\Users\13632\.claude\ai-api.log`
2. 健康检查：`.claude\check-api-health.bat`
3. 参考文档：`.claude\AI_MODELS_GUIDE.md`
